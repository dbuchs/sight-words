services:
  web:
    build: .
    container_name: sight-words
    expose:
      - "8000"
    networks:
      - web
    volumes:
      - db_data:/data
    env_file:
      - /home/deploy/secrets/common.env
    restart: unless-stopped

volumes:
  db_data:

networks:
  web:
    external: true
