services:
  web:
    build: .
    container_name: sight-words
    expose:
      - "8000"
    networks:
      - web
    env_file:
      - ~/secrets/common.env
    restart: unless-stopped

networks:
  web:
    external: true
